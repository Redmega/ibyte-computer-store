machine:
  timezone:
    America/New_York
  php:
    version: 7.0.7
  node:
    version: 6.1.0
  environment:
    APP_ENV: testing
dependencies:
  post:
    - php artisan migrate --seed --force --no-interaction --env=testing
